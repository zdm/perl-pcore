#!/usr/bin/env perl

package main v0.1.0;

use Pcore qw[-embedded];
use Pcore::Src;
use Pcore::Core::CLI1;

P->file->chdir( $PROC->{START_DIR} );

Pcore::Core::CLI1->new( { class => 'Pcore::Src' } )->run( \@ARGV );

__END__
=pod

=encoding utf8

=head1 NAME

src - source formatter

=head1 DESCRIPTION

=over

=back

=cut
