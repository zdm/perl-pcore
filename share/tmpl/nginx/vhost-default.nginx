# default server
server {
    listen                    *:80 default_server reuseport;
    listen                    [::]:80 default_server reuseport;
    server_name               "";
    return                    444;
}
